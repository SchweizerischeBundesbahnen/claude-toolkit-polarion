# CLAUDE.md

- `plugins/core/CLAUDE.md` is redistributed to end-users when they install the core plugin — changes there affect users, not just this repo.
- Conventional commits are enforced (`feat:`, `fix:`, `chore:`, etc.) — wrong format blocks the commit.
